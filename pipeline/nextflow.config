process {
    maxForks = 50
    queueSize = 50
    resourceLabels = ['allen-batch-pipeline': 'aind-disrnn-pipeline']
}

env.CO_COMPUTATION_ID = "$CO_COMPUTATION_ID"

// process.resourceLabels = ['allen-batch-pipeline': 'aind-disrnn-pipeline']